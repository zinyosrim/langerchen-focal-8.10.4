{% comment %}
Remove color from product title, e.g. Yoga Basic Top (Pink) ->  Yoga Basic Top
1. split by )
2. take first part
3. split by (
4. take first part
5. remove trailing spaces
{% endcomment %}
Farbe: {{ product_name | split: "(" | last | split: ")" | first | strip }}       
<span class="sold_out">{% if product.available == false %}{{ 'products.product.sold_out' | t }}{% endif %}</span>
